#ifndef _H_Light#define _H_Light#include "BasicStructures.h"/* Header for the Sphere class. */class Light{private:	/* - - - - - - - - - - - - ATTRIBUTES - - - - - - - - - -*/	/* The centre and the intensity of the light. */	point centre;		double intensity;	/* The colour of this sphere. */	double r, g, b;public:	/* - - - - - - - CONSTRUCTOR & DESTRUCTOR - - - - - - - -*/	/* Constructor & destructor. */	explicit Light(double x, double y, double z, double in, double rC, double gC, double bC);	~Light();	/* - - - - - - - OTHER METHODS - - - - - - - -*/		/* - - - - - - - GETTERS & SETTERS - - - - - - - -*/	point getCentre();	double getIntensity();	double getR();	double getG();	double getB();};#endif